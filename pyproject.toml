[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pro-evo-open-core"
version = "0.1.0"
description = "Public-safe open core and offline evidence replay for Pro-Evo"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

