# Public event projection

The pair records in `../summary.json` are the canonical sanitized event projection. Event IDs are deliberately opaque research identities. No source paths, provider payloads, prompts, private reasoning, test code, expected patches, or hidden-oracle outputs are published.

