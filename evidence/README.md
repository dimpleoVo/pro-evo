# Public evidence

These are small, structured projections of frozen research results. They are evidence for the stated claims, not an export of the private runtime or its raw artifacts. The Gate22 replay is fully offline and recomputes the public aggregate from the sanitized pair records.

