# Same-checkpoint demo

The Gate22 pair records are a deterministic reference example. Every pair asserts identical opaque checkpoint identities for G and T before their branches diverge. Run `python examples/evidence-replay/run.py` from the repository root.

