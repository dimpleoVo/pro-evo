# Optimization model

An optimization target is a compact public abstraction: an opaque target ID, a public diagnosis category, and an observable success condition. This release never publishes the private controller or instructions used to execute a treatment. It publishes only the target-linked evidence necessary to audit the claimed outcome.

