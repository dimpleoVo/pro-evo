# Evaluation model

`target_resolved` records whether the defined observable target is satisfied. `verified_completion` records successful public reverification. A branch may revise a workspace without satisfying either metric. Final success alone is therefore insufficient to establish target-linked reliability in this release.

