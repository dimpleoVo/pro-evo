# Causal validation

For every Gate22 pair, G and T share the same pre-revision checkpoint ID. The post-checkpoint branch is the comparison unit. The public replay verifies checkpoint equality before aggregating outcome metrics and complete treatment chains. This controls the public starting state for each pair; it does not remove the stated scope limitations.

